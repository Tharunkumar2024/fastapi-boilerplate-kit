"""
This module provides users seed.

By default no users are seeded. Add fixture users here if your project needs them.
Initial SuperAdmin bootstrap is handled by initial_admin_seeder when auth is enabled.
"""

from sqlalchemy.orm import Session


def seed_users(db: Session) -> None:
    """Placeholder for optional fixture users (roles-only bootstrap by default)."""
    _ = db
